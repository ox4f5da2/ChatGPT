const HELLO = "我是 ChatGPT 聊天机器人，欢迎和我聊天😊！";

const INTRO = "ding"; // 添加好友时的确认消息

const ERROR = "目前不支持回复除文本消息之外的内容🥹"; // 当发送除文本之外的内容的提示

const NAME = "快乐的 AI (*^ω^*)"; // 机器人的名字

const config = {
  HELLO,
  INTRO,
  ERROR,
  NAME,
}

export default config;